// Sample c++ code  (GCC 11.x compiler)
#include<iostream>  // Preprocessor directive

using namespace std; //Use the std Namespace globally

int main(int argn, char* argv[]) {          // Main function
    int h=10;                               // Integer declation and initialisation
    cout << h << " is a number\n";          // cout -> stands for console output
    cout << argn << " command line argument recived\n";
    return 0;
}
